# NoticeMate backend dependencies
# Core web framework
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Data / validation
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
python-dotenv>=1.0

# AI (optional at runtime — the app degrades gracefully to a deterministic
# "Demo analysis" fallback when this is unavailable or no API key is set)
openai>=1.30

# Testing
pytest>=8.0
httpx>=0.27
